# 🗝️ 7번방의 기록 — 사용 가이드

직업상담사용 사전 상담 게임. 구직자가 7개의 방을 통과하며 자신의 경험(키워드·에피소드)을
기록하면, 상담사는 구글시트로 결과를 받아 상담 자료로 활용합니다.

---

## 📁 파일 구성

| 파일 | 역할 | 수정해도 되나요? |
|---|---|---|
| **content.js** | 문항·방·점수 규칙·화면 문구 (콘텐츠 전부) | ✅ **여기만 수정하세요** |
| engine.js | 점수 계산·판정 로직 | ❌ 수정 금지 |
| app.js | 게임 화면 | ❌ 수정 금지 |
| index.html | 게임 실행 파일 (구직자가 여는 페이지) | ❌ 수정 금지 |
| test.html / tests.js | 자동 테스트 (수정 후 검사 도구) | ❌ 수정 금지 |
| ncs-data.js | NCS 직무 데이터 (자동 생성) | ❌ 직접 수정 금지 — 갱신은 아래 참고 |
| univ-index.js + univ/ 폴더 | 전국 대학·학과·교과목 데이터 (자동 생성) | ❌ 직접 수정 금지 — 갱신은 아래 참고 |
| corp-list.js | 국내 상장사 목록 (자동 생성, 희망 기업 검색용) | ❌ 직접 수정 금지 |

> ⚠️ **구글시트 연동 코드(apps-script.gs)는 `_학습조직` 폴더로 옮겨졌습니다** —
> DART 인증키가 들어 있어 배포되는 게임 폴더에 두면 안 되기 때문입니다.
> 절대 게임 폴더 안으로 다시 옮기지 마세요.

> **공공데이터 갱신**: 데이터가 개정되면(연 1~2회) `_학습조직` 폴더의 스크립트를 터미널에서 실행하세요.
> - NCS 직무: `node ncs-데이터갱신.js`
> - 대학 학과·교과목: `node 학과-데이터갱신.js`
>
> ⚠️ 이 갱신 스크립트들에는 공공데이터 인증키가 들어 있으니 게임 폴더에 넣거나 외부에 공유하지 마세요.

---

## ✏️ 1. 문항 수정하는 방법

`content.js` 파일을 메모장(또는 VS Code)으로 열어 수정합니다.

### 질문 내용 바꾸기
```js
{ id: "r1q1", type: "text",
  text: "여기 질문 내용을 바꾸면 됩니다" },
```

### 문항 추가하기 — 방 안의 questions 목록에 한 줄 추가
```js
{ id: "r1q5", type: "text", text: "새로 추가한 질문입니다" },
```
- `id`는 다른 문항과 **절대 겹치면 안 됩니다** (r1q5, r1q6 처럼 이어서 붙이면 안전)
- `type`은 다섯 가지:
  - `"list"` — 여러 칸 채우기 (`count: 3`으로 칸 수 지정. 예: 과목 3가지)
  - `"short"` — 단어·한 문장 속답
  - `"text"` — 서술형 답변
  - `"episode"` — 에피소드 카드 작성/재사용
  - `"ncs"` — NCS 직무 검색해서 담기 (국가직무능력표준 1,094개 직무 자동완성.
    담으면 "그 직무가 하는 일"이 표시되고, 시트에 NCS 코드가 함께 기록됨)
  - `"univ"` — 학교·학과 검색 후 그 학과의 실제 교과목 목록에서 골라 담기
    (전국 1,670개교 · 16,384개 학과. 학교가 없거나 고졸이면 "직접 입력" 모드 제공.
    인트로에서 학교·학과를 골랐다면 자동으로 반영되고, 희망 직무와 관련된 과목엔 ⭐ 표시)
  - `"pick"` — 보기 칩에서 골라 담기 (`options: [...]`로 보기 지정, `allowCustom: true`면
    직접 추가 가능, `optionsFrom: "profileJobs"`면 인트로에서 고른 희망 직무가 보기가 됨)

> **희망 기업 카드**: 인트로에서 상장사를 고르면 금융감독원 공시(DART) 숫자로
> 기업 카드(매출·성장률·직원·근속·급여)가 표시되고, 방마다 기업 힌트가 나옵니다
> (문구는 `texts.companyHints.frames`). 7번 방에서는 그 회사 숫자로 만든 면접 질문을
> 골라 담는 문항이 나오고, 최종 화면의 **📄 리포트 저장(PDF)** 에 기업분석 표와 함께 담깁니다.
>
> **직무 힌트 카드**: 인트로에서 희망 직무를 고르면 각 방의 질문창에 그 직무의
> NCS 능력 키워드가 힌트로 표시됩니다. 방별 연결 문구는 content.js 의
> `texts.jobHints.frames` 에서 수정하고, 문구를 지우면 그 방에선 힌트가 꺼집니다.
> 에피소드 중복 의심 민감도는 `settings.episodeSimilarity` (기본 0.45, 낮출수록 민감).
- `intent` — 상담사용 메모(이 질문의 의도). 구직자에게는 안 보이고 구글시트에 함께 기록됨
- `placeholder` — 입력 칸의 흐린 예시 문구 (선택)
- 방 안의 아이템은 **위에서부터 순서대로 잠금 해제**되므로, 앞 질문의 답을 이어받는
  질문("그중 최고 하나는?")을 만들 수 있습니다

### 문항 삭제하기
- 해당 `{ ... },` 한 덩어리를 통째로 지우면 됩니다.

### 방 추가/삭제, 점수 규칙 변경
- 방 전체(`{ id: "room1", ... }` 덩어리)를 복사/삭제하면 방 개수가 바뀝니다.
- 파일 상단 `settings`에서 배지 커트라인(60점), 진실의 방 기준(배지 3개),
  보너스 점수 등을 숫자만 바꾸면 됩니다.
- **화면과 점수 계산은 자동으로 새 구성에 맞춰집니다.** 코드 수정 불필요!

### ⚠️ 수정 후 반드시 할 일
> **test.html 을 브라우저로 열어서 초록색 "모든 테스트 통과"를 확인하세요.**
> 빨간색이 나오면 어디가 잘못됐는지 한글로 알려줍니다. (id 중복, 오타 등)
> 초록색이면 안심하고 배포해도 됩니다.

---

## 🌐 2. 게임 배포하기 (구직자에게 링크/QR 보내기)

게임은 서버가 필요 없는 정적 웹페이지라 무료 호스팅에 올리면 끝납니다.

### 가장 쉬운 방법: Netlify Drop
1. https://app.netlify.com/drop 접속 (무료 가입)
2. 이 폴더 전체를 마우스로 끌어다 놓기
3. 생성된 주소(예: `https://ooo.netlify.app`)가 게임 링크
4. 문항 수정 후에는 같은 방법으로 다시 끌어다 놓으면 갱신

### 링크 배포 팁
- **상담사 구분**: 링크 뒤에 `?c=상담사코드`를 붙여 배포하면
  구글시트에 상담사코드가 함께 기록됩니다.
  예: `https://ooo.netlify.app?c=kim01`
- **QR 만들기**: 네이버에 "QR코드 만들기" 검색 → 링크 붙여넣기 → 이미지 저장

---

## 📊 3. 구글시트 연동 (결과 받아보기)

1. [구글시트](https://sheets.google.com) 새 문서 만들기 (이름 예: "7번방의 기록 결과")
2. 메뉴 **확장 프로그램 > Apps Script** 클릭
3. 기존 코드를 모두 지우고 `apps-script.gs` 파일 내용 전체를 붙여넣기 → 💾 저장
4. 오른쪽 위 **배포 > 새 배포** 클릭
   - 유형 선택(⚙️): **웹 앱**
   - 실행 계정: **나**
   - 액세스 권한: **모든 사용자** ← 꼭 이걸로!
   - [배포] 클릭 → 권한 승인 (경고가 떠도 "고급 > 이동"으로 승인)
5. 생성된 **웹 앱 URL** 복사
6. `content.js` 파일을 열어 붙여넣기:
   ```js
   sheetEndpoint: "https://script.google.com/macros/s/....../exec",
   ```
7. 게임을 다시 배포(Netlify에 다시 드롭)하면 연동 완료

### 시트에 쌓이는 내용
- **요약 시트**: 구직자 1명 = 1행. 배지 수, 총점, **⚠️ 진실의 방 대상 여부**,
  수집한 에피소드 목록, **취조 리스트(부실하게 답한 문항 목록)**
- **답변상세 시트**: 모든 문항의 답변 원문, 에피소드 재사용 여부(⚠️ 표시)

> 상담 전에 요약 시트 한 행만 훑어봐도: 배지가 몇 개인지 → 진실의 방 대상인지 →
> 어떤 문항을 부실하게 답했는지(= 상담에서 파고들 지점)가 한눈에 보입니다.

---

## 🎮 4. 게임 규칙 요약 (구직자 안내용)

- 7개의 방을 순서대로 통과 (방마다 3~5문항, 총 20~30분)
- 답변 점수: 기본 10점 + 성실(50자↑) 5점 + 결과·숫자 포함 5점 + 새 에피소드 10점
- **낙서 차단**: 완성된 한글 문장만 인정 — "dfdf", "ㅇㄹㅇㄹ"(자음만), "가가가가"(도배),
  "아무말아무말"(반복)은 무응답 0점 처리. "ㅋㅋㅋ" 도배는 글자 수에서 제외.
  (기준 조정: content.js 의 minHangulSyllables, sincereBonusChars)
- 방 점수 60점 이상 → 배지 획득 (총 7개)
- **같은 에피소드를 다른 방에서 재사용하면 그 문항은 에피소드 점수 없음**
- 배지 3개 미만 → 상담일에 "진실의 방"이 기다립니다 🚪
- 중간에 꺼져도 같은 기기·같은 브라우저로 다시 열면 이어하기 가능

---

## 🔧 문제 해결

| 증상 | 해결 |
|---|---|
| 수정했는데 게임이 옛날 방식으로 동작함 | 브라우저가 옛 파일을 기억(캐시)하는 것. **Ctrl+Shift+R(강력 새로고침)** 또는 모바일은 브라우저 기록 삭제. 인트로 화면 맨 아래 **버전 표시**(v2.1.0)로 최신 여부 확인 가능. Netlify에 올린 경우엔 폴더를 다시 드롭해야 반영됨 |
| 게임 화면에 "게임 설정 오류"가 뜸 | content.js 수정 중 실수. test.html 을 열면 원인이 표시됨 |
| 시트에 결과가 안 들어옴 | ① sheetEndpoint 주소 확인 ② Apps Script 배포 시 액세스 "모든 사용자"인지 확인 ③ 코드 수정 후엔 "배포 > 배포 관리 > 새 버전"으로 재배포 필요 |
| 구직자가 처음부터 다시 하고 싶어함 | 최종 결과 화면의 "처음부터 다시 하기" 버튼 |
